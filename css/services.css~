/* services.css - Services Section Styles */

.services-section {
    padding: var(--section-padding);
    position: relative;
    background: linear-gradient(180deg, transparent, rgba(0, 28, 76, 0.03), transparent);
}

/* Updated Grid Layout - Featured in first row, 3 cards in second row */
.services-grid {
    display: grid;
    grid-template-columns: 1fr;
    grid-template-rows: auto auto;
    gap: 30px;
    margin-top: 60px;
    max-width: 1000px;
    margin-left: auto;
    margin-right: auto;
}

/* Second row with 3 equal cards */
.services-row-2 {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 25px;
    margin-top: 30px;
}

.service-card {
    padding: 40px 30px;
    position: relative;
    transition: var(--transition-smooth);
    cursor: pointer;
    height: 100%;
    display: flex;
    flex-direction: column;
    background: rgba(0, 28, 76, 0.04); /* Blue background */
    border: 1px solid rgba(0, 28, 76, 0.15);
    box-shadow: var(--blue-glow);
}

.service-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, transparent, rgba(0, 28, 76, 0.08));
    border-radius: 20px;
    opacity: 0;
    transition: opacity 0.3s ease;
}

.service-card:hover::before {
    opacity: 1;
}

/* Featured Card - Further reduced height for better proportion */
.service-card.featured {
    min-height: 220px; /* Reduced from 280px to 220px */
    background: linear-gradient(135deg, rgba(0, 28, 76, 0.08), rgba(0, 28, 76, 0.02));
    border-color: var(--penn-blue);
    box-shadow: var(--blue-glow-strong);
}

/* Regular cards - Slightly reduced height for better proportion */
.service-card:not(.featured) {
    min-height: 320px; /* Reduced from 350px */
}

.featured-badge {
    position: absolute;
    top: -15px;
    right: 30px;
    background: var(--mustard);
    color: var(--black);
    padding: 5px 15px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    box-shadow: var(--mustard-glow);
}

.service-icon {
    width: 70px;
    height: 70px;
    background: linear-gradient(135deg, rgba(0, 28, 76, 0.1), rgba(0, 28, 76, 0.05));
    border: 1px solid rgba(0, 28, 76, 0.2);
    border-radius: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 25px;
    transition: var(--transition-smooth);
    box-shadow: var(--blue-glow);
}

.service-icon i {
    font-size: 30px;
    color: var(--mustard);
    transition: var(--transition-smooth);
    text-shadow: var(--mustard-glow);
}

.service-card:hover .service-icon {
    transform: translateY(-5px) rotate(5deg);
    background: linear-gradient(135deg, rgba(0, 28, 76, 0.2), rgba(0, 28, 76, 0.1));
    box-shadow: var(--blue-glow-strong);
}

.service-card:hover .service-icon i {
    transform: scale(1.1);
    text-shadow: var(--mustard-glow-strong);
}

.service-card h3 {
    font-size: 24px;
    margin-bottom: 15px;
    color: var(--alabaster);
    text-shadow: var(--white-glow);
}

/* Adjust featured card title size and spacing */
.service-card.featured h3 {
    font-size: 26px; /* Reduced from 28px */
    margin-bottom: 12px; /* Reduced spacing */
}

.service-card.featured p {
    font-size: 15px;
    margin-bottom: 20px; /* Reduced spacing */
}

.service-card.featured .service-features {
    margin-bottom: 20px; /* Reduced spacing */
}

.service-card.featured .service-features li {
    padding: 6px 0; /* Reduced padding */
}

.service-card p {
    font-size: 15px;
    line-height: 1.7;
    opacity: 0.8;
    margin-bottom: 25px;
    flex-grow: 1;
}

.service-features {
    list-style: none;
    margin-bottom: 30px;
}

.service-features li {
    padding: 8px 0;
    padding-left: 25px;
    position: relative;
    font-size: 14px;
    opacity: 0.9;
}

.service-features li::before {
    content: '→';
    position: absolute;
    left: 0;
    color: var(--mustard);
    font-weight: bold;
    text-shadow: var(--mustard-glow);
}

.service-cta {
    background: transparent;
    border: 1px solid rgba(0, 28, 76, 0.3);
    color: var(--mustard);
    padding: 12px 24px;
    border-radius: 30px;
    font-size: 14px;
    font-weight: 500;
    cursor: pointer;
    transition: var(--transition-smooth);
    display: inline-flex;
    align-items: center;
    gap: 8px;
    align-self: flex-start;
    text-shadow: var(--mustard-glow);
    box-shadow: var(--blue-glow);
}

.service-cta i {
    font-size: 12px;
    transition: transform 0.3s ease;
}

.service-cta:hover {
    background: rgba(0, 28, 76, 0.1);
    border-color: var(--penn-blue);
    padding-right: 30px;
    box-shadow: var(--blue-glow-strong);
    text-shadow: var(--mustard-glow-strong);
}

.service-cta:hover i {
    transform: translateX(5px);
}

/* Service Card Animations */
.service-card[data-service="digital"] {
    animation: slideInUp 0.6s ease-out;
}

.service-card[data-service="social"] {
    animation: slideInUp 0.6s ease-out 0.1s backwards;
}

.service-card[data-service="creative"] {
    animation: slideInUp 0.6s ease-out 0.2s backwards;
}

.service-card[data-service="analytics"] {
    animation: slideInUp 0.6s ease-out 0.3s backwards;
}

/* Hover Effects for Different Services - Blue themed with enhanced glow */
.service-card[data-service="social"]:hover {
    box-shadow: 0 20px 60px rgba(0, 28, 76, 0.3), var(--blue-glow-strong);
}

.service-card[data-service="digital"]:hover {
    box-shadow: 0 20px 60px rgba(0, 28, 76, 0.35), var(--blue-glow-strong);
}

.service-card[data-service="creative"]:hover {
    box-shadow: 0 20px 60px rgba(0, 28, 76, 0.3), var(--blue-glow-strong);
}

.service-card[data-service="analytics"]:hover {
    box-shadow: 0 20px 60px rgba(0, 28, 76, 0.3), var(--blue-glow-strong);
}

/* Responsive */
@media (max-width: 968px) {
    .services-grid {
        grid-template-columns: 1fr;
    }

    .services-row-2 {
        grid-template-columns: 1fr;
        gap: 20px;
    }

    .service-card.featured {
        min-height: auto;
    }

    .service-card:not(.featured) {
        min-height: auto;
    }
}

@media (max-width: 640px) {
    .services-row-2 {
        grid-template-columns: 1fr;
    }

    .service-card {
        min-height: auto;
        padding: 30px 25px;
    }

    .service-icon {
        width: 60px;
        height: 60px;
    }

    .service-icon i {
        font-size: 24px;
    }
}